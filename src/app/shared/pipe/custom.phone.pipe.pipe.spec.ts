import { Custom.Phone.PipePipe } from './custom.phone.pipe.pipe';

describe('Custom.Phone.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Custom.Phone.PipePipe();
    expect(pipe).toBeTruthy();
  });
});
