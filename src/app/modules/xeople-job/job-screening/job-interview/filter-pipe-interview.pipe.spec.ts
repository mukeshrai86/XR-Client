import { FilterPipeInterviewPipe } from './filter-pipe-interview.pipe';

describe('FilterPipeInterviewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeInterviewPipe();
    expect(pipe).toBeTruthy();
  });
});
