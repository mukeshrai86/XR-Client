import { AssessmentPipe } from './assessment.pipe';

describe('AssessmentPipe', () => {
  it('create an instance', () => {
    const pipe = new AssessmentPipe();
    expect(pipe).toBeTruthy();
  });
});
